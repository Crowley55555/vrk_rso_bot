# Локальная разработка: API + Telegram + Max одним `pip install -r requirements.txt`.
# В Docker у каждого сервиса свой минимальный набор — см. api/requirements.txt,
# telegram_bot/requirements.txt, max_bot/requirements.txt.

# --- API (FastAPI + Google Sheets) ---
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
gspread>=6.0.0
google-auth-oauthlib>=1.2.0
pydantic>=2.0,<3

# --- Боты ---
python-telegram-bot>=21.8,<22.0
httpx>=0.27.0,<1.0

# --- Общее ---
python-dotenv>=1.0.1
