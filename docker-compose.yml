version: "3.9"

services:
  bot:
    build: .
    container_name: vrk_rso_bot
    restart: unless-stopped
    working_dir: /app
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
    command: ["python", "-m", "bot.main"]

